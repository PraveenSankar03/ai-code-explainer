# AI Explainer

Django + React application that explains concepts using Gemini AI.

## Setup

### Backend

cd backend
pip install -r requirements.txt
python manage.py runserver

### Frontend

cd frontend
npm install
npm start

## Environment Variables

Create:

backend/.env

Add:

GEMINI_API_KEY=your_api_key